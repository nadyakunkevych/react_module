export default function User ({item}) {

    const onClick = () => {
        (user.id)
    }
    return (
        <div>
            {item.name}
            <button onClick={onClick}> posts </button>

        </div>
    )
}