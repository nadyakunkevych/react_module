import React from 'react';

const User = ({item}) => {
    return (
        <div>
            {item.name}
            <button onClick={()=> details </button>

        </div>
    );


export default User;